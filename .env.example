# Copy to .env and adjust as needed. All keys are optional; defaults live in
# app/core/config.py.

APP_NAME=fast_back
ENVIRONMENT=development
DEBUG=true

# Comma-separated allowed CORS origins (JSON list also accepted by pydantic).
CORS_ORIGINS=["*"]
